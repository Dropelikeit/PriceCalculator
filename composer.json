{
  "name": "marcel-strahl/price-calculator",
  "description": "An service to calculate prices",
  "keywords": ["price", "calculator"],
  "type": "library",
  "minimum-stability": "stable",
  "license": "MIT",
  "authors": [
    {
      "name": "Marcel Strahl",
      "email": "info@marcel-strahl.de"
    }
  ],
  "require": {
    "php": ">=7.4",
    "ext-bcmath": "*"
  },
  "require-dev": {
    "friendsofphp/php-cs-fixer": "^2.16",
    "phpstan/phpstan": "^0.12.9",
    "phpstan/phpstan-phpunit": "^0.12.6",
    "phpunit/phpunit": "^9.0"
  },
  "autoload": {
    "psr-4": {
      "MarcelStrahl\\PriceCalculator\\": "src/",
      "MarcelStrahl\\PriceCalculator\\Tests\\": "tests/"
    }
  },
  "config": {
    "platform": {
      "php": "7.4"
    },
    "sort-packages": true
  },
  "prefer-stable": true,
  "scripts": {
    "cs-check": "php-cs-fixer -v --dry-run --using-cache=no fix",
    "cs-fix": "php-cs-fixer --using-cache=no fix",
    "test": "phpunit --configuration ~/PriceCalculator/phpunit.xml",
    "analyze": "vendor/bin/phpstan analyse --level 7 --configuration phpstan.neon.dist --error-format baselineNeon",
    "analyze-add": "vendor/bin/phpstan analyse --level 7 --configuration phpstan.neon.dist --error-format baselineNeon  src/ tests/ > phpstan-baseline.neon",
    "check": [
      "@cs-check",
      "@analyze",
      "@test"
    ]
  }
}
